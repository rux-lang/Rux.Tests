import Std::String;
import Std::Platform::*;

func Main() -> int {
    var str = String::From("Hello, world!");
    PrintPointer("{}\n", str.data);
    PrintInt64("{}\n", str.length as int64);
    return 0;
}
